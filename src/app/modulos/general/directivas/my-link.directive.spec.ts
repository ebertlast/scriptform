import { MyLinkDirective } from './my-link.directive';

describe('MyLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new MyLinkDirective();
    expect(directive).toBeTruthy();
  });
});
